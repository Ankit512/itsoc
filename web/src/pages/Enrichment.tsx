import { Intel } from "./Intel";

/** @deprecated Merged into Intel (Live Enrichment section) per Phase C1 (C1-T3). */
export function Enrichment() {
  return <Intel defaultSection="enrichment" />;
}
