import { Network } from "./Network";

/** @deprecated Retired in C1-T4 — merged into Network with tab-based navigation. */
export function Vulnerabilities() {
  return <Network defaultTab="vulnerabilities" />;
}
