.env
__pycache__/
*.pyc
sample_report.*

# threat-intel demo artifacts (regenerated by the commands in threat_intel/README.md)
demo_report.*
demo_iocs.txt
demo_threat_report.*

# compare-mode ablation cache (local inference results, machine-specific)
.compare_cache.json

# adapter output when written next to the console (serve.py keeps it in memory)
console/console_state.json

# saved run history for the console (local results, machine-specific)
console/.runs/
console/.soc/

# graphify (local tooling: knowledge graph, project skill, hooks)
graphify-out/
.claude/

# itsoc_mcp caches
itsoc_mcp/__pycache__/
itsoc_mcp/_tmp_sample.log
itsoc_mcp/_tmp_ti/
itsoc_mcp/dist/
itsoc_mcp/build/
itsoc_mcp/*.egg-info/

# --- housekeeping (cruft cleanup) ---
__pycache__/
*.pyc
*Zone.Identifier
*.backup*
report.json
report.md
state.json
node_modules
web/node_modules

# --- fleet infrastructure & foreign queues (HK-1) ---
inbox/
hive/
.hive/
fleet.json
registry.json
log.jsonl
cost-ledger.jsonl
tasks.json
board.md
hooks.sock
spawn-requests/
agents/

.DS_Store
